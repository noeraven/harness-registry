## Extension listing

- Extension ID:
- Public repository URL:
- Reason for adding or changing this listing:

## Checks

- [ ] `noeraven-extension.json` is at the repository root and its ID matches the registry entry.
- [ ] The repository URL is public and uses `https://github.com/OWNER/REPOSITORY`.
- [ ] Version, name, description, commands, and themes remain in the extension manifest.
- [ ] I reviewed the contributed prompts and theme colors.
- [ ] `node --test` and `node scripts/validate.mjs` pass.
